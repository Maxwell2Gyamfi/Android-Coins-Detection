package com.example.coinsdetection

class History(thumbnail: Int, numItems: Int, totalAmount: Double) {
    public var thumbnail = thumbnail
    public var numItems = numItems
    public var totalAmount = totalAmount
}